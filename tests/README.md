# Tests

This folder contains pytest-style checks.

The FFS objective work adds focused checks for structured versus unstructured DNS weighting in `test_field_error.py`. Standalone FFS plotting and diagnostics live under `scripts/ffs/`.
