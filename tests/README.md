# Tests

This folder contains pytest-style checks and standalone plotting helpers.

The FFS objective work adds focused checks for structured versus unstructured DNS weighting in `test_field_error.py`. Plot configs live under `tests/plots/` and are independent of the main experiment configs.
