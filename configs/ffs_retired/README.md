# Retired FFS Configs

Historical C_SEP-only and diagnostic configurations are stored here for
provenance. They are not production inputs and may reference obsolete fields,
paths, objectives, or boundary-condition behavior.

Use the configurations under `configs/ffs_final/` for final FFS optimization.
